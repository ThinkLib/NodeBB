'use strict';

var db = require('../../database');

var async = require('async');
var winston = require('winston');

module.exports = {
	name: 'User friendly upgrade script name',
	// remember, month is zero-indexed (so Jauary is 0, December is 11)
	timestamp: Date.UTC(2017, 0, 1),
	method: function (callback) {
		// Do stuff here...
	},
};
